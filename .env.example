# ---------------------------------------------------------
# Static Assets Stack Configuration
# ---------------------------------------------------------

# The domain where the script will be served
SERVICE_DOMAIN=scripts.yourdomain.com

# The external docker network Traefik resides on
DOCKER_NETWORK=traefik_proxy

# The target portal domain that is permitted to load the script via CORS
ALLOWED_ORIGIN=https://portal.yourdomain.com
